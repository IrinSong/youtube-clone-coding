extends ../layout
include ../mixins/video

block content 
  div.profile__data
    img(src=user.avatarUrl).avatar
    h3=user.name
    if !loggedIn
    else if String(loggedInUser._id) === String(id)
      a(href="/users/edit").edit_profile__link 프로필 수정
  div.video-grid
    each video in user.videos
      +info(video)
    else
      span.empty__message 업로드한 비디오가 없습니다.