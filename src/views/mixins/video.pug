mixin info(video)
  div
    h4
      a(href=`/videos/${video.id}`)=video.title
      //- attribute(href, class, id ..)에는 #{} 사용이 불가능.
    p=video.description
    each hashtag in video.hashtags
      li=hashtag
    small=video.createdAt
    hr